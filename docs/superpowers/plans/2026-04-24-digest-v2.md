# Digest v2 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 瘦身推送正文（去掉"关键人物与数据"和"影响与展望"两段），让 detail 结构化更易读，语音稿按篇计字支持更长内容，并重构代码合并 main.py / main_hot.py、把 prompt 外挂为独立文件。

**Architecture:** `AnalyzedArticle` 删 2 字段（`key_people_and_data`、`impact`）；两条 prompt 从模块里抽到 `src/prompts/*.md` 文件，改文案不用改 Python；新增 `src/pipeline.py` 用 `mode` 参数区分 digest/hot 两条流水线，`main.py` / `main_hot.py` 瘦身为入口。

**Tech Stack:** Python 3.12、pytest、pytest-asyncio、Playwright、httpx、OpenAI-compatible SDK、edge-tts。

**Spec:** `docs/superpowers/specs/2026-04-24-digest-v2-design.md`

---

## File Structure

### Create
- `src/prompts/analyzer.md` — analyzer LLM system prompt
- `src/prompts/audio_scripter.md` — audio scripter LLM system prompt
- `src/pipeline.py` — 统一 digest/hot 流水线的 `run_pipeline(*, mode, config)`
- `tests/test_pipeline.py` — 参数化测试 digest 和 hot 两种 mode

### Modify
- `src/models.py` — `AnalyzedArticle` 删 `key_people_and_data`、`impact`
- `src/analyzer.py` — 从文件加载 `_SYSTEM_PROMPT`
- `src/audio_scripter.py` — 从文件加载 prompt、source 字典只填 7 个字段
- `src/notifier.py` — `format_digest` section 模板删 2 段
- `main.py` — 瘦身为入口（logging 初始化 + 调用 `run_pipeline`）
- `main_hot.py` — 瘦身为入口（同上，mode="hot"）
- `tests/test_analyzer.py` — fixture 和断言删 2 字段
- `tests/test_notifier.py` — 删 2 段断言，新增加粗段首原样保留的断言
- `tests/test_audio_scripter.py` — fixture 删 2 字段、断言改匹配新 prompt 关键短语、断言 source 字典只含 7 字段

### Delete
- `test_cookie.py`（repo 根）
- `test_page_structure.py`（repo 根）
- `test_comment_structure.py`（repo 根）
- `test_llm_live.py`（repo 根）
- `test_edge_tts_live.py`（repo 根）
- `tests/test_main.py`（被 `tests/test_pipeline.py` 取代）
- `tests/test_main_hot.py`（被 `tests/test_pipeline.py` 取代）

---

## Task 1: 建分支

**Files:** 无

- [ ] **Step 1: 从 main 建新分支**

```bash
git checkout main
git pull --ff-only
git checkout -b refactor/digest-v2
```

- [ ] **Step 2: 确认分支干净**

```bash
git status
```
Expected: `On branch refactor/digest-v2` + `nothing to commit, working tree clean`

---

## Task 2: 瘦身 AnalyzedArticle 数据模型

**Files:**
- Modify: `src/models.py`
- Modify: `tests/test_analyzer.py`
- Modify: `tests/test_notifier.py`
- Modify: `tests/test_audio_scripter.py`
- Modify: `tests/test_main.py`
- Modify: `tests/test_main_hot.py`

- [ ] **Step 1: 更新 `tests/test_analyzer.py` 的 fixture 和断言**

把 `test_returns_analyzed_articles` 改为：

```python
    def test_returns_analyzed_articles(self):
        mock_llm = MagicMock()
        mock_llm.complete.return_value = json.dumps([{
            "title_cn": "阿森纳主导比赛",
            "title_original": "Arsenal dominate",
            "article_type": "深度分析",
            "importance": 5,
            "overview": "阿森纳在比赛中展现了统治力。",
            "detail": "**战术转折：** 详细的战术分析内容。",
            "link": "https://example.com/1",
        }])
        articles = [_make_article()]
        result = analyze_articles(articles, mock_llm)
        assert len(result) == 1
        assert result[0].title_cn == "阿森纳主导比赛"
        assert result[0].importance == 5
        assert not hasattr(result[0], "key_people_and_data")
        assert not hasattr(result[0], "impact")
```

- [ ] **Step 2: 更新 `tests/test_notifier.py` 的 `_make_analyzed`**

改成：

```python
def _make_analyzed() -> AnalyzedArticle:
    return AnalyzedArticle(
        title_cn="阿森纳争冠分析", title_original="Arsenal title race analysis",
        article_type="深度分析", importance=5,
        overview="阿森纳在本赛季展现了强大的争冠实力。",
        detail="**战术转折：** 详细的战术分析。",
        link="https://example.com/1",
    )
```

- [ ] **Step 3: 更新 `tests/test_audio_scripter.py` 的 `_make_article`**

改成：

```python
def _make_article(title_cn="阿森纳主导比赛", title_original="Arsenal dominate") -> AnalyzedArticle:
    return AnalyzedArticle(
        title_cn=title_cn,
        title_original=title_original,
        article_type="深度分析",
        importance=5,
        overview="阿森纳在比赛中展现了统治力。",
        detail="详细的战术分析内容。",
        link="https://example.com/1",
    )
```

（这一步暂时会破坏 `test_calls_llm_with_expected_prompt_contract` 中对 `key_people_and_data` 和 `impact` 的断言 —— 没关系，Task 4 会重写这个测试。）

- [ ] **Step 4: 更新 `tests/test_main.py` 的所有 `AnalyzedArticle(...)` fixture**

把三处 `AnalyzedArticle(...)` 构造（分别位于 `test_full_pipeline`、`test_audio_enabled_generates_and_sends_audio_url`、`test_audio_failure_falls_back_to_text_only`、`test_audio_disabled_skips_audio_branch`）统一改为：

```python
analyzed = AnalyzedArticle(
    title_cn="测试", title_original="Test", article_type="新闻",
    importance=5, overview="概述", detail="详情",
    link="http://x",
)
```

- [ ] **Step 5: 同样更新 `tests/test_main_hot.py` 中的 `AnalyzedArticle(...)` fixture**

三处 `AnalyzedArticle(...)` 改为同上（去掉 `key_people_and_data` 和 `impact`）。

- [ ] **Step 6: 运行测试，确认因为 dataclass 字段还没删所以失败**

Run: `pytest tests/ -v`
Expected: 绝大部分测试仍然 PASS（因为删掉的是 fixture 里的 kwargs，dataclass 还有字段但有默认值？不，没默认值会失败）—— 实际上因为 `AnalyzedArticle` 字段没默认值，删掉 kwargs 会 FAIL with `TypeError: __init__() missing 2 required positional arguments: 'key_people_and_data' and 'impact'`

- [ ] **Step 7: 更新 `src/models.py`，删除 `AnalyzedArticle` 的 2 个字段**

```python
from dataclasses import dataclass
from datetime import datetime


@dataclass
class Article:
    title: str
    link: str
    summary: str
    published: datetime
    full_text: str = ""
    comment_count: int = 0


@dataclass
class AnalyzedArticle:
    title_cn: str
    title_original: str
    article_type: str
    importance: int
    overview: str
    detail: str
    link: str
```

- [ ] **Step 8: 再跑测试，确认 `test_analyzer.py`、`test_notifier.py`、`test_main.py`、`test_main_hot.py` 相关用例通过；`test_audio_scripter.py` 的 prompt 断言仍然失败（Task 4 处理）**

Run: `pytest tests/test_analyzer.py tests/test_notifier.py tests/test_main.py tests/test_main_hot.py -v`
Expected: PASS

Run: `pytest tests/test_audio_scripter.py -v`
Expected: 部分 FAIL（`test_calls_llm_with_expected_prompt_contract` 断言里还在检查 `key_people_and_data` 和 `impact` —— 这是预期的，Task 4 一起重写）

- [ ] **Step 9: Commit**

```bash
git add src/models.py tests/test_analyzer.py tests/test_notifier.py tests/test_audio_scripter.py tests/test_main.py tests/test_main_hot.py
git commit -m "refactor(models): drop key_people_and_data and impact from AnalyzedArticle"
```

---

## Task 3: 把 analyzer prompt 抽到独立文件

**Files:**
- Create: `src/prompts/analyzer.md`
- Modify: `src/analyzer.py`
- Modify: `tests/test_analyzer.py`

- [ ] **Step 1: 新增测试验证 prompt 是从文件加载的（TDD）**

在 `tests/test_analyzer.py` 末尾加：

```python
class TestAnalyzerPrompt:
    def test_system_prompt_loaded_from_file(self):
        from src.analyzer import _SYSTEM_PROMPT
        assert "**加粗段首：**" in _SYSTEM_PROMPT
        assert "保留英文原文" in _SYSTEM_PROMPT
        assert "key_people_and_data" not in _SYSTEM_PROMPT
        assert "impact" not in _SYSTEM_PROMPT
```

- [ ] **Step 2: 运行该测试，确认失败**

Run: `pytest tests/test_analyzer.py::TestAnalyzerPrompt -v`
Expected: FAIL（当前 `_SYSTEM_PROMPT` 里不含 `**加粗段首：**` 这些新内容）

- [ ] **Step 3: 创建 `src/prompts/` 目录并写入 analyzer.md**

```bash
mkdir -p src/prompts
```

创建文件 `src/prompts/analyzer.md`，内容：

````markdown
你是一位资深英超足球记者和分析师。请对以下英超文章进行深度中文分析。

核心原则：尽量保留原文的丰富内容，不要过度精简。读者希望通过你的分析获取接近原文的信息量，而不仅仅是摘要。

返回一个 JSON 对象，仅包含以下字段：
- title_cn: 中文标题翻译
- title_original: 英文原标题
- article_type: 文章类型（深度分析/新闻报道/战术解读/转会动态/赛后分析）
- importance: 重要性 1-5
- overview: 3-5 句话概述核心论点，独立成立——读完这一段就能知道发生了什么、作者的核心判断是什么（中文）
- detail: 深度转述原文内容（中文）
- link: 原文链接

detail 字段的写作要求：

1. 长度：深度分析类 800-1200 字，普通新闻 400-600 字。宁可不到下限也不要注水。
2. 结构：分 3-5 个小段，每段用 `**加粗段首：**` 起头（如 `**战术转折：**`、`**数据与对比：**`、`**球员状态：**`、`**背景与影响：**`）。不要使用任何 Markdown 标题符号（不要用 #、##、###、####）。
3. 引语：原文中的直接引语用独立一行的 `> ` 起头，不要把引语揉进叙述段落里。
4. 重点：关键数据、比分、转折性事实用 `**粗体**` 标出（例如 **3-2**、**xG 2.41**、**本赛季第 12 球**）。
5. 人名与专名：首次出现的球员名、主教练名、俱乐部名、赛事名保留英文原文，可在其后括注中文（如 `Erling Haaland（哈兰德）`）；同一实体在 detail 中再次出现时，用英文或中文任一种均可，但保持一致。比分始终用数字形式（3-2，而非"三比二"）。
6. 内容覆盖：把原文中的关键人物、核心数据、战术细节、影响与展望有机地融入各段落，不要专设"人物"或"影响"段落——这些信息应该分散在 3-5 个 `**加粗段首：**` 小段里。不要为了结构而结构，段落划分服从内容逻辑。

仅返回 JSON 对象，不要添加任何其他文字，不要包裹 ``` 代码块。
````

- [ ] **Step 4: 改 `src/analyzer.py` 从文件加载 prompt**

把文件头部改为：

```python
import json
import logging
from dataclasses import fields
from pathlib import Path

from src.llm import LLMClient
from src.models import AnalyzedArticle, Article

logger = logging.getLogger(__name__)

_ANALYZED_FIELDS = {f.name for f in fields(AnalyzedArticle)}

_SYSTEM_PROMPT = (Path(__file__).parent / "prompts" / "analyzer.md").read_text(encoding="utf-8")
```

删除原来硬编码的 `_SYSTEM_PROMPT = """..."""` 多行字符串。其余函数 `_parse_response`、`analyze_articles` 保持不变。

- [ ] **Step 5: 跑 analyzer 所有测试**

Run: `pytest tests/test_analyzer.py -v`
Expected: PASS (all)

- [ ] **Step 6: Commit**

```bash
git add src/prompts/analyzer.md src/analyzer.py tests/test_analyzer.py
git commit -m "refactor(analyzer): extract prompt to src/prompts/analyzer.md"
```

---

## Task 4: 把 audio_scripter prompt 抽到文件 + 精简 source 字典

**Files:**
- Create: `src/prompts/audio_scripter.md`
- Modify: `src/audio_scripter.py`
- Modify: `tests/test_audio_scripter.py`

- [ ] **Step 1: 重写 `tests/test_audio_scripter.py` 的 prompt 契约测试**

把 `test_calls_llm_with_expected_prompt_contract` 和新 prompt 测试整体替换为：

```python
    def test_calls_llm_with_expected_prompt_contract(self):
        llm = MagicMock()
        llm.complete.return_value = "口播稿"
        article = _make_article()

        build_audio_script([article], llm, date(2026, 4, 23))

        llm.complete.assert_called_once()
        system_text, user_text = llm.complete.call_args[0]
        assert "开场" in system_text
        assert "转场" in system_text
        assert "结尾" in system_text
        assert "每篇文章 500-700" in system_text
        assert "保留英文人名" in system_text
        assert "不要使用 Markdown" in system_text
        assert "title_prefix: 英超早报" in user_text
        assert "year: 2026" in user_text
        assert "month: 4" in user_text
        assert "day: 23" in user_text
        assert "articles:" in user_text
        assert "- article 1" in user_text
        assert "title_cn: 阿森纳主导比赛" in user_text
        assert "title_original: Arsenal dominate" in user_text
        assert "article_type: 深度分析" in user_text
        assert "importance: 5" in user_text
        assert "overview: 阿森纳在比赛中展现了统治力。" in user_text
        assert "detail: 详细的战术分析内容。" in user_text
        assert "link: https://example.com/1" in user_text
        assert "key_people_and_data" not in user_text
        assert "impact" not in user_text
```

- [ ] **Step 2: 跑测试，确认因为 prompt 还没改所以失败**

Run: `pytest tests/test_audio_scripter.py::TestBuildAudioScript::test_calls_llm_with_expected_prompt_contract -v`
Expected: FAIL（断言 `"每篇文章 500-700" in system_text` 失败，当前 prompt 是 `1500-2500`）

- [ ] **Step 3: 创建 `src/prompts/audio_scripter.md`**

```markdown
你是一名中文足球音频节目撰稿人。请根据给定资讯整理成自然、流畅、适合直接口播的中文播报稿。

要求：

1. 结构：开场问候 + 逐篇播报（每篇之间要有自然转场） + 结尾收束。
2. 长度：每篇文章 500-700 汉字，整稿长度按文章数量自然上浮（例如 5 篇 ≈ 2500-3500 字，3 篇 ≈ 1500-2100 字）。不要为了凑字数注水，也不要草草带过。
3. 内容：把每篇 overview 和 detail 的关键论据、数据、引语、战术细节展开讲，像主播在给听众解读，而不是读稿。可以适度加入过渡性评论（"这里值得注意的是……"、"换句话说……"），但不要编造原文没有的事实。
4. 人名与专名：保留英文人名、队名、赛事名、比分原样，不要误译或省略（如 Haaland、Manchester City、3-2、Champions League）。中文听众能听懂英文读音，刻意翻译反而会失真。
5. 风格：像主播在说话——简洁、自然、有节奏感。不要使用 Markdown、项目符号、URL，不要出现"概述"、"详情"、"影响"等书面标签。避免长句堆砌，多用短句和口语化连接词。
6. 输出：只返回播报稿正文，不要任何元信息、标题或说明。
```

- [ ] **Step 4: 改 `src/audio_scripter.py`，从文件加载 prompt 并精简 source 字典**

整个文件替换为：

```python
import logging
from datetime import date
from pathlib import Path

from src.llm import LLMClient
from src.models import AnalyzedArticle

logger = logging.getLogger(__name__)

_SYSTEM_PROMPT = (Path(__file__).parent / "prompts" / "audio_scripter.md").read_text(encoding="utf-8")


def build_audio_script(
    articles: list[AnalyzedArticle],
    llm: LLMClient,
    today: date,
    title_prefix: str = "英超早报",
) -> str:
    if not articles:
        raise ValueError("no articles")

    sections = [
        f"title_prefix: {title_prefix}",
        f"year: {today.year}",
        f"month: {today.month}",
        f"day: {today.day}",
        "articles:",
    ]

    for index, article in enumerate(articles, start=1):
        sections.extend(
            [
                f"- article {index}",
                f"title_cn: {article.title_cn}",
                f"title_original: {article.title_original}",
                f"article_type: {article.article_type}",
                f"importance: {article.importance}",
                f"overview: {article.overview}",
                f"detail: {article.detail}",
                f"link: {article.link}",
            ]
        )

    user_text = "\n".join(sections)
    logger.info("Building audio script for %d articles", len(articles))
    return llm.complete(_SYSTEM_PROMPT, user_text, operation="build_audio_script").strip()
```

- [ ] **Step 5: 跑测试**

Run: `pytest tests/test_audio_scripter.py -v`
Expected: PASS (all 5)

- [ ] **Step 6: Commit**

```bash
git add src/prompts/audio_scripter.md src/audio_scripter.py tests/test_audio_scripter.py
git commit -m "refactor(audio): extract prompt to file and drop key_people/impact source"
```

---

## Task 5: 更新 notifier 去掉 2 段 + 加粗段首保留断言

**Files:**
- Modify: `src/notifier.py`
- Modify: `tests/test_notifier.py`

- [ ] **Step 1: 给 `tests/test_notifier.py` 加断言**

在 `TestFormatDigest` 类里，把 `test_formats_markdown` 改为：

```python
    def test_formats_markdown(self):
        articles = [_make_analyzed()]
        title, body = format_digest(articles, date(2026, 2, 15))
        assert "英超每日精选" in title
        assert "2026-02-15" in title
        assert "阿森纳争冠分析" in body
        assert "深度分析" in body
        assert "⭐⭐⭐⭐⭐" in body
        assert "### 文章概述" in body
        assert "### 详细内容" in body
        assert "关键人物与数据" not in body
        assert "影响与展望" not in body

    def test_preserves_bold_section_markers_in_detail(self):
        article = AnalyzedArticle(
            title_cn="标题", title_original="Title",
            article_type="深度分析", importance=3,
            overview="概述",
            detail="**战术转折：** 前半场被动。\n\n> 教练说：'我们必须做出改变。'\n\n**数据与对比：** **xG 2.41**。",
            link="https://example.com/1",
        )
        _, body = format_digest([article], date(2026, 2, 15))
        assert "**战术转折：**" in body
        assert "> 教练说：" in body
        assert "**xG 2.41**" in body
```

- [ ] **Step 2: 运行测试，确认 2 个新断言失败（旧的 body 还含"关键人物与数据"/"影响与展望"段）**

Run: `pytest tests/test_notifier.py::TestFormatDigest -v`
Expected: `test_formats_markdown` FAIL（旧模板仍含 2 段，断言 `"关键人物与数据" not in body` 失败）

- [ ] **Step 3: 改 `src/notifier.py` 的 section 模板**

把 `format_digest` 中 `section = f"""..."""` 块替换为：

```python
        section = f"""## {i}. {a.title_cn}
**原标题：** {a.title_original}
**类型：** {a.article_type}
**重要性：** {stars}

### 文章概述
{a.overview}

### 详细内容
{a.detail}

🔗 [阅读原文]({a.link})"""
```

- [ ] **Step 4: 跑测试**

Run: `pytest tests/test_notifier.py -v`
Expected: PASS (all)

- [ ] **Step 5: Commit**

```bash
git add src/notifier.py tests/test_notifier.py
git commit -m "refactor(notifier): drop key_people and impact sections from digest"
```

---

## Task 6: 新增 `src/pipeline.py` 并用 TDD 写参数化测试

**Files:**
- Create: `src/pipeline.py`
- Create: `tests/test_pipeline.py`

- [ ] **Step 1: 写 `tests/test_pipeline.py`（TDD）**

完整内容：

```python
import asyncio
from unittest.mock import patch, MagicMock, AsyncMock
from datetime import datetime, timezone

import pytest

from src.models import Article, AnalyzedArticle


def _make_config(audio_enabled: bool = False) -> dict:
    return {
        "page_url": "http://page",
        "athletic_cookies": [],
        "llm_base_url": "https://api.example.com",
        "llm_api_key": "key",
        "llm_model": "test-model",
        "serverchan_key": "sc-key",
        "top_n": 5,
        "audio_enabled": audio_enabled,
        "audio_voice": "zh-CN-YunjianNeural",
        "audio_keep_releases": 7,
    }


def _make_article() -> Article:
    return Article(
        title="Test", link="http://x", summary="S",
        published=datetime(2026, 2, 15, tzinfo=timezone.utc),
        comment_count=42,
    )


def _make_analyzed() -> AnalyzedArticle:
    return AnalyzedArticle(
        title_cn="测试", title_original="Test", article_type="新闻",
        importance=5, overview="概述", detail="详情",
        link="http://x",
    )


class TestRunPipelineDigest:
    @patch("src.pipeline.save_step")
    @patch("src.pipeline.notify")
    @patch("src.pipeline.analyze_articles")
    @patch("src.pipeline.scrape_full_texts", new_callable=AsyncMock)
    @patch("src.pipeline.rank_articles")
    @patch("src.pipeline.collect_articles", new_callable=AsyncMock)
    def test_full_digest_pipeline_calls_rank(
        self, mock_collect, mock_rank, mock_scrape, mock_analyze, mock_notify, mock_save
    ):
        from src.pipeline import run_pipeline

        mock_collect.return_value = [_make_article()]
        mock_rank.return_value = [_make_article()]
        mock_scrape.return_value = ([_make_article()], False)
        mock_analyze.return_value = [_make_analyzed()]
        mock_notify.return_value = True

        asyncio.run(run_pipeline(mode="digest", config=_make_config()))

        mock_collect.assert_awaited_once()
        mock_rank.assert_called_once()
        mock_scrape.assert_awaited_once()
        mock_analyze.assert_called_once()
        mock_notify.assert_called_once()
        assert mock_notify.call_args.kwargs["title_prefix"] == "英超每日精选"

    @patch("src.pipeline.save_step")
    @patch("src.pipeline.notify")
    @patch("src.pipeline.collect_articles", new_callable=AsyncMock)
    def test_digest_no_articles_skips_notify(self, mock_collect, mock_notify, mock_save):
        from src.pipeline import run_pipeline

        mock_collect.return_value = []
        asyncio.run(run_pipeline(mode="digest", config=_make_config()))

        mock_notify.assert_not_called()


class TestRunPipelineHot:
    @patch("src.pipeline.save_step")
    @patch("src.pipeline.notify")
    @patch("src.pipeline.analyze_articles")
    @patch("src.pipeline.scrape_full_texts", new_callable=AsyncMock)
    @patch("src.pipeline.rank_articles")
    @patch("src.pipeline.collect_hot_articles", new_callable=AsyncMock)
    def test_full_hot_pipeline_skips_rank(
        self, mock_collect, mock_rank, mock_scrape, mock_analyze, mock_notify, mock_save
    ):
        from src.pipeline import run_pipeline

        mock_collect.return_value = [_make_article()]
        mock_scrape.return_value = ([_make_article()], False)
        mock_analyze.return_value = [_make_analyzed()]
        mock_notify.return_value = True

        asyncio.run(run_pipeline(mode="hot", config=_make_config()))

        mock_collect.assert_awaited_once()
        mock_rank.assert_not_called()
        mock_scrape.assert_awaited_once()
        mock_analyze.assert_called_once()
        assert mock_notify.call_args.kwargs["title_prefix"] == "英超热议文章"


class TestRunPipelineAudio:
    @patch("src.pipeline.save_step")
    @patch("src.pipeline.notify")
    @patch("src.pipeline.synthesize_and_upload", new_callable=AsyncMock)
    @patch("src.pipeline.build_audio_script")
    @patch("src.pipeline.analyze_articles")
    @patch("src.pipeline.scrape_full_texts", new_callable=AsyncMock)
    @patch("src.pipeline.rank_articles")
    @patch("src.pipeline.collect_articles", new_callable=AsyncMock)
    def test_digest_audio_uses_audio_digest_tag(
        self, mock_collect, mock_rank, mock_scrape, mock_analyze,
        mock_script, mock_synth, mock_notify, mock_save,
    ):
        from src.pipeline import run_pipeline

        mock_collect.return_value = [_make_article()]
        mock_rank.return_value = [_make_article()]
        mock_scrape.return_value = ([_make_article()], False)
        mock_analyze.return_value = [_make_analyzed()]
        mock_script.return_value = "script"
        mock_synth.return_value = "https://example.com/a.mp3"
        mock_notify.return_value = True

        asyncio.run(run_pipeline(mode="digest", config=_make_config(audio_enabled=True)))

        assert mock_synth.await_args.kwargs["tag_prefix"] == "audio-digest"
        assert mock_notify.call_args.kwargs["audio_url"] == "https://example.com/a.mp3"

    @patch("src.pipeline.save_step")
    @patch("src.pipeline.notify")
    @patch("src.pipeline.synthesize_and_upload", new_callable=AsyncMock)
    @patch("src.pipeline.build_audio_script")
    @patch("src.pipeline.analyze_articles")
    @patch("src.pipeline.scrape_full_texts", new_callable=AsyncMock)
    @patch("src.pipeline.collect_hot_articles", new_callable=AsyncMock)
    def test_hot_audio_uses_audio_hot_tag(
        self, mock_collect, mock_scrape, mock_analyze,
        mock_script, mock_synth, mock_notify, mock_save,
    ):
        from src.pipeline import run_pipeline

        mock_collect.return_value = [_make_article()]
        mock_scrape.return_value = ([_make_article()], False)
        mock_analyze.return_value = [_make_analyzed()]
        mock_script.return_value = "script"
        mock_synth.return_value = "https://example.com/h.mp3"
        mock_notify.return_value = True

        asyncio.run(run_pipeline(mode="hot", config=_make_config(audio_enabled=True)))

        assert mock_synth.await_args.kwargs["tag_prefix"] == "audio-hot"
        assert mock_notify.call_args.kwargs["title_prefix"] == "英超热议文章"

    @patch("src.pipeline.save_step")
    @patch("src.pipeline.notify")
    @patch("src.pipeline.synthesize_and_upload", new_callable=AsyncMock)
    @patch("src.pipeline.build_audio_script")
    @patch("src.pipeline.analyze_articles")
    @patch("src.pipeline.scrape_full_texts", new_callable=AsyncMock)
    @patch("src.pipeline.rank_articles")
    @patch("src.pipeline.collect_articles", new_callable=AsyncMock)
    def test_audio_failure_falls_back_to_text_only(
        self, mock_collect, mock_rank, mock_scrape, mock_analyze,
        mock_script, mock_synth, mock_notify, mock_save,
    ):
        from src.pipeline import run_pipeline

        mock_collect.return_value = [_make_article()]
        mock_rank.return_value = [_make_article()]
        mock_scrape.return_value = ([_make_article()], False)
        mock_analyze.return_value = [_make_analyzed()]
        mock_script.return_value = "script"
        mock_synth.side_effect = RuntimeError("boom")
        mock_notify.return_value = True

        asyncio.run(run_pipeline(mode="digest", config=_make_config(audio_enabled=True)))

        assert mock_notify.call_args.kwargs["audio_url"] is None
```

- [ ] **Step 2: 跑测试确认失败（模块还不存在）**

Run: `pytest tests/test_pipeline.py -v`
Expected: FAIL with `ModuleNotFoundError: No module named 'src.pipeline'`

- [ ] **Step 3: 创建 `src/pipeline.py`**

完整内容：

```python
import logging
import os
import sys
from dataclasses import asdict
from datetime import date
from typing import Literal

from src.analyzer import analyze_articles
from src.audio_scripter import build_audio_script
from src.audio_synth import synthesize_and_upload
from src.collector import collect_articles
from src.config import save_step
from src.hot_collector import collect_hot_articles
from src.llm import LLMClient
from src.notifier import notify
from src.ranker import rank_articles
from src.scraper import scrape_full_texts

logger = logging.getLogger(__name__)

_MODE_DEFAULTS = {
    "digest": {
        "title_prefix": "英超每日精选",
        "tag_prefix": "audio-digest",
        "output_subdir": None,
    },
    "hot": {
        "title_prefix": "英超热议文章",
        "tag_prefix": "audio-hot",
        "output_subdir": "hot",
    },
}


async def run_pipeline(*, mode: Literal["digest", "hot"], config: dict) -> None:
    if mode not in _MODE_DEFAULTS:
        raise ValueError(f"unknown mode: {mode}")
    opts = _MODE_DEFAULTS[mode]

    today = date.today()
    output_parts = ["output"]
    if opts["output_subdir"]:
        output_parts.append(opts["output_subdir"])
    output_parts.append(str(today))
    output_dir = os.path.join(*output_parts)

    logger.info("Step 1: Collecting articles (mode=%s)...", mode)
    if mode == "digest":
        articles = await collect_articles(config["page_url"], config["athletic_cookies"])
    else:
        articles = await collect_hot_articles(
            config["page_url"], config["athletic_cookies"], top_n=config["top_n"]
        )
    if not articles:
        logger.info("No articles found. Exiting.")
        return

    logger.info("Found %d articles", len(articles))
    save_step("step1_collected", [asdict(a) for a in articles], output_dir)

    llm = LLMClient(
        base_url=config["llm_base_url"],
        api_key=config["llm_api_key"],
        model=config["llm_model"],
    )

    if mode == "digest":
        logger.info("Step 2: Ranking articles...")
        articles = rank_articles(articles, llm, top_n=config["top_n"])
        logger.info("Selected top %d articles", len(articles))
        save_step("step2_ranked", [asdict(a) for a in articles], output_dir)

    logger.info("Step 3: Scraping full texts...")
    articles, has_fallbacks = await scrape_full_texts(articles, config["athletic_cookies"])
    save_step(
        "step3_scraped",
        [{**asdict(a), "full_text": a.full_text[:200] + "..."} for a in articles],
        output_dir,
    )

    logger.info("Step 4: Analyzing articles...")
    analyzed = analyze_articles(articles, llm)
    save_step("step4_analyzed", [asdict(a) for a in analyzed], output_dir)
    if not analyzed:
        logger.error("Analysis failed, no results to push")
        return

    audio_url: str | None = None
    if config["audio_enabled"]:
        try:
            logger.info("Step 4a: Generating audio script...")
            script = build_audio_script(analyzed, llm, today, title_prefix=opts["title_prefix"])
            save_step("step4a_script", {"script": script}, output_dir)

            logger.info("Step 4b: Synthesizing and uploading audio...")
            audio_url = await synthesize_and_upload(
                script,
                today,
                voice=config["audio_voice"],
                tag_prefix=opts["tag_prefix"],
                keep=config["audio_keep_releases"],
            )
            save_step("step4b_audio", {"url": audio_url}, output_dir)
        except Exception as e:
            logger.error("Audio pipeline failed, falling back to text-only: %s", e)
            audio_url = None

    logger.info("Step 5: Pushing to WeChat...")
    success = notify(
        analyzed,
        config["serverchan_key"],
        today=today,
        title_prefix=opts["title_prefix"],
        has_fallbacks=has_fallbacks,
        audio_url=audio_url,
    )
    if success:
        logger.info("Pipeline (%s) sent successfully!", mode)
    else:
        logger.error("Failed to send pipeline (%s)", mode)
        sys.exit(1)
```

注意 `build_audio_script` 原签名是 `title_prefix="英超早报"`，这里改为显式传入 mode 的 prefix（digest 传 `英超每日精选`、hot 传 `英超热议文章`）；这会改变语音稿开场白内容。如果需要保留原 `英超早报` 作为 digest 模式的默认，改回 `build_audio_script(analyzed, llm, today)` 即可（不传 title_prefix）。本计划按 spec 默认用各自的 `title_prefix`。

- [ ] **Step 4: 跑 pipeline 测试**

Run: `pytest tests/test_pipeline.py -v`
Expected: PASS (all 6)

- [ ] **Step 5: Commit**

```bash
git add src/pipeline.py tests/test_pipeline.py
git commit -m "feat(pipeline): add unified run_pipeline for digest and hot modes"
```

---

## Task 7: 瘦身 `main.py` 和 `main_hot.py`

**Files:**
- Modify: `main.py`
- Modify: `main_hot.py`

- [ ] **Step 1: 重写 `main.py`**

完整替换为：

```python
import asyncio
import logging

from src.config import get_config
from src.pipeline import run_pipeline

logging.basicConfig(
    level=logging.INFO,
    format="%(asctime)s [%(levelname)s] %(name)s: %(message)s",
)


if __name__ == "__main__":
    asyncio.run(run_pipeline(mode="digest", config=get_config()))
```

- [ ] **Step 2: 重写 `main_hot.py`**

完整替换为：

```python
import asyncio
import logging

from src.config import get_config
from src.pipeline import run_pipeline

logging.basicConfig(
    level=logging.INFO,
    format="%(asctime)s [%(levelname)s] %(name)s: %(message)s",
)


if __name__ == "__main__":
    asyncio.run(run_pipeline(mode="hot", config=get_config()))
```

- [ ] **Step 3: 跑完整测试套件**

Run: `pytest tests/ -v`
Expected: PASS (新的 test_pipeline.py 通过、其余模块级测试通过；旧的 `tests/test_main.py` 和 `tests/test_main_hot.py` 会因为 `from main import run` 找不到 `run` 函数而失败 —— Task 8 删除）

- [ ] **Step 4: 确认新 main 可以导入**

Run: `python -c "import main; import main_hot; print('ok')"`
Expected: `ok`

- [ ] **Step 5: Commit**

```bash
git add main.py main_hot.py
git commit -m "refactor(main): slim entry points to call run_pipeline"
```

---

## Task 8: 删除旧测试文件和根目录调试脚本

**Files:**
- Delete: `tests/test_main.py`, `tests/test_main_hot.py`
- Delete: `test_cookie.py`, `test_page_structure.py`, `test_comment_structure.py`, `test_llm_live.py`, `test_edge_tts_live.py`

- [ ] **Step 1: 删除被 test_pipeline.py 取代的旧测试**

```bash
git rm tests/test_main.py tests/test_main_hot.py
```

- [ ] **Step 2: 删除根目录遗留调试脚本**

```bash
git rm test_cookie.py test_page_structure.py test_comment_structure.py test_llm_live.py test_edge_tts_live.py
```

- [ ] **Step 3: 跑完整测试套件，确认全绿**

Run: `pytest tests/ -v`
Expected: PASS (全部用例，无 FAIL 无 ERROR)

- [ ] **Step 4: Commit**

```bash
git commit -m "chore: remove legacy root debug scripts and superseded pipeline tests"
```

---

## Task 9: 更新 CLAUDE.md 架构描述

**Files:**
- Modify: `CLAUDE.md`

- [ ] **Step 1: 更新 `CLAUDE.md` 的 Architecture 段**

把原来的 `main.py` / `main_hot.py` 描述合并，并更新 `src/` 文件清单：

找到这一段：

```
main.py              # Digest pipeline: collect → rank → scrape → analyze → optional audio → notify
main_hot.py          # Hot pipeline: collect (sorted by comments) → scrape → analyze → optional audio → notify
```

替换为：

```
main.py              # Digest entry: calls run_pipeline(mode="digest")
main_hot.py          # Hot entry: calls run_pipeline(mode="hot")
src/pipeline.py      # Unified pipeline: mode routes collect/rank/title_prefix/tag_prefix differences
```

并在 `src/` 下的文件列表里把 `src/pipeline.py` 加进去（紧接 `config.py` 后面），`prompts/` 目录也顺便提一句：

```
  prompts/           # External LLM system prompts (analyzer.md, audio_scripter.md)
```

- [ ] **Step 2: Commit**

```bash
git add CLAUDE.md
git commit -m "docs: update architecture for pipeline refactor and prompt externalization"
```

---

## Task 10: 全量验证

**Files:** 无

- [ ] **Step 1: 跑完整测试套件**

Run: `pytest tests/ -v`
Expected: 所有测试 PASS，无 skipped 除非原来就有

- [ ] **Step 2: 确认 main 入口可调用**

Run: `python -c "import main; import main_hot; from src.pipeline import run_pipeline; print('imports ok')"`
Expected: `imports ok`

- [ ] **Step 3: 确认 prompt 文件加载**

Run: `python -c "from src.analyzer import _SYSTEM_PROMPT as a; from src.audio_scripter import _SYSTEM_PROMPT as b; print(len(a), len(b))"`
Expected: 两个都是非零字符数（大约 600-1000）

- [ ] **Step 4: 检查 git 历史干净**

```bash
git log --oneline main..refactor/digest-v2
```
Expected: 8-9 个 conventional commits，按 Task 顺序排列

- [ ] **Step 5: （可选，需要真实 API key）本地 live 跑一次 digest**

```bash
python main.py
```
Expected: 推送成功，肉眼检查：
- 微信消息里没有"关键人物与数据"和"影响与展望"两段
- detail 里出现 `**XXX：**` 加粗段首、`> ` 引语、`**粗体**` 数据
- 音频链接可播放，播报内容比以前长

如果发现 LLM 不听 prompt 指令（比如 detail 还是密排、没加粗段首），迭代修改 `src/prompts/analyzer.md` 文案，无需改代码。

---

## Self-Review Notes

- ✅ Spec coverage：5 个 Goals 全部有对应 Task（1 → Task 5、2 → Task 3、3 → Task 4、4 → Task 3/4/6/7/8、5 → Task 3/4 的外挂 prompt）。
- ✅ 无 placeholder / TODO / TBD。
- ✅ 类型和方法签名前后一致：`run_pipeline(*, mode, config)` 在 Task 6 定义、Task 7 调用一致；`build_audio_script` 签名未变。
- ✅ TDD 顺序：Task 3/4/5/6 都是先写/改测试、跑 FAIL、再写实现、跑 PASS。Task 2 的 TDD 顺序反过来（先改 fixture 让测试失败，再改模型让测试通过），是因为这是字段删除而非新功能，方向一致但叙述反转。
- ✅ 每个 Task 结束都有 commit，分支历史清晰。
